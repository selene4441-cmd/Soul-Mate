# Soulmate

匹配 Agent（MVP）项目骨架。

## Quickstart

1. 创建并激活虚拟环境（可选）
2. 安装依赖：`pip install -r requirements.txt`
3. 复制环境变量：`cp .env.example .env`（Windows 可手动复制）
4. 启动服务：`uvicorn app.main:app --reload`

## 常用命令

- `pytest -q`
- `ruff check .`
- `uvicorn app.main:app --reload`

